apiVersion: argoproj.io/v1alpha1
kind: Workflow
metadata:
  generateName: fedpod-jobs-
spec:
  entrypoint: parallel-steps
  arguments:
    parameters:
    - name: rounds
      value: "15" # mode selection role: (cen: 00, sol: 01, fed: >= 02)
    - name: initial-round
      value: "00"
    - name: job-prefix
      value: "fed1R15"
    - name: split_csv
      value: "cc359ppmi128/CC359PPMI_v1.csv"
  templates:
  - name: parallel-steps
    steps:
  # round 0
    - - name: run-train-jobs-00
        template: train-job
        arguments:
          parameters:
          - name: round
            value: "{{workflow.parameters.initial-round}}"
          - name: epochs
            value: "{{item.epochs}}"
          - name: epoch
            value: "{{item.epoch}}"
          - name: inst-id
            value: "{{item.inst}}"
          - name: model_pth
            value: "None"
        withItems:
        - { epochs: "002", epoch: "000", inst: "1" }
        - { epochs: "002", epoch: "000", inst: "2" }
        - { epochs: "002", epoch: "000", inst: "3" }
        - { epochs: "002", epoch: "000", inst: "4" }
        - { epochs: "002", epoch: "000", inst: "5" }
        - { epochs: "002", epoch: "000", inst: "6" }
    - - name: run-aggregation-jobs-00
        template: aggregation-job
        arguments:
          parameters:
          - name: round
            value: "{{workflow.parameters.initial-round}}"
          - name: algorithm
            value: "fedavg"
          - name: inst-id
            value: "0"
          - name: model_pth
            value: "None"
  # round 1
    - - name: run-train-jobs-01
        template: train-job
        arguments:
          parameters:
          - name: round
            value: "{{steps.run-aggregation-jobs-00.outputs.parameters.next-round}}"
          - name: epochs
            value: "{{item.epochs}}"
          - name: epoch
            value: "{{item.epoch}}"
          - name: inst-id
            value: "{{item.inst}}"
          - name: model_pth
            value: "/fedpod/states/{{workflow.parameters.job-prefix}}_0/R{{workflow.parameters.rounds}}r{{steps.run-aggregation-jobs-00.outputs.parameters.next-round}}/models/R{{workflow.parameters.rounds}}r{{steps.run-aggregation-jobs-00.outputs.parameters.next-round}}.pth"
        withItems:
        - { epochs: "002", epoch: "002", inst: "1" }
        - { epochs: "002", epoch: "002", inst: "2" }
        - { epochs: "002", epoch: "002", inst: "3" }
        - { epochs: "002", epoch: "002", inst: "4" }
        - { epochs: "002", epoch: "002", inst: "5" }
        - { epochs: "002", epoch: "002", inst: "6" }
    - - name: run-aggregation-jobs-01
        template: aggregation-job
        arguments:
          parameters:
          - name: round
            value: "{{steps.run-aggregation-jobs-00.outputs.parameters.next-round}}"
          - name: algorithm
            value: "fedavg"
          - name: inst-id
            value: "0"
          - name: model_pth
            value: "None"
  # round 2
    - - name: run-train-jobs-02
        template: train-job
        arguments:
          parameters:
          - name: round
            value: "{{steps.run-aggregation-jobs-01.outputs.parameters.next-round}}"
          - name: epochs
            value: "{{item.epochs}}"
          - name: epoch
            value: "{{item.epoch}}"
          - name: inst-id
            value: "{{item.inst}}"
          - name: model_pth
            value: "/fedpod/states/{{workflow.parameters.job-prefix}}_0/R{{workflow.parameters.rounds}}r{{steps.run-aggregation-jobs-01.outputs.parameters.next-round}}/models/R{{workflow.parameters.rounds}}r{{steps.run-aggregation-jobs-01.outputs.parameters.next-round}}.pth"
        withItems:
        - { epochs: "002", epoch: "004", inst: "1" }
        - { epochs: "002", epoch: "004", inst: "2" }
        - { epochs: "002", epoch: "004", inst: "3" }
        - { epochs: "002", epoch: "004", inst: "4" }
        - { epochs: "002", epoch: "004", inst: "5" }
        - { epochs: "002", epoch: "004", inst: "6" }
    - - name: run-aggregation-jobs-02
        template: aggregation-job
        arguments:
          parameters:
          - name: round
            value: "{{steps.run-aggregation-jobs-01.outputs.parameters.next-round}}"
          - name: algorithm
            value: "fedavg"
          - name: inst-id
            value: "0"
          - name: model_pth
            value: "None"
  # round 3
    - - name: run-train-jobs-03
        template: train-job
        arguments:
          parameters:
          - name: round
            value: "{{steps.run-aggregation-jobs-02.outputs.parameters.next-round}}"
          - name: epochs
            value: "{{item.epochs}}"
          - name: epoch
            value: "{{item.epoch}}"
          - name: inst-id
            value: "{{item.inst}}"
          - name: model_pth
            value: "/fedpod/states/{{workflow.parameters.job-prefix}}_0/R{{workflow.parameters.rounds}}r{{steps.run-aggregation-jobs-02.outputs.parameters.next-round}}/models/R{{workflow.parameters.rounds}}r{{steps.run-aggregation-jobs-02.outputs.parameters.next-round}}.pth"
        withItems:
        - { epochs: "002", epoch: "006", inst: "1" }
        - { epochs: "002", epoch: "006", inst: "2" }
        - { epochs: "002", epoch: "006", inst: "3" }
        - { epochs: "002", epoch: "006", inst: "4" }
        - { epochs: "002", epoch: "006", inst: "5" }
        - { epochs: "002", epoch: "006", inst: "6" }
    - - name: run-aggregation-jobs-03
        template: aggregation-job
        arguments:
          parameters:
          - name: round
            value: "{{steps.run-aggregation-jobs-02.outputs.parameters.next-round}}"
          - name: algorithm
            value: "fedavg"
          - name: inst-id
            value: "0"
          - name: model_pth
            value: "None"
  # round 4
    - - name: run-train-jobs-04
        template: train-job
        arguments:
          parameters:
          - name: round
            value: "{{steps.run-aggregation-jobs-03.outputs.parameters.next-round}}"
          - name: epochs
            value: "{{item.epochs}}"
          - name: epoch
            value: "{{item.epoch}}"
          - name: inst-id
            value: "{{item.inst}}"
          - name: model_pth
            value: "/fedpod/states/{{workflow.parameters.job-prefix}}_0/R{{workflow.parameters.rounds}}r{{steps.run-aggregation-jobs-03.outputs.parameters.next-round}}/models/R{{workflow.parameters.rounds}}r{{steps.run-aggregation-jobs-03.outputs.parameters.next-round}}.pth"
        withItems:
        - { epochs: "002", epoch: "008", inst: "1" }
        - { epochs: "002", epoch: "008", inst: "2" }
        - { epochs: "002", epoch: "008", inst: "3" }
        - { epochs: "002", epoch: "008", inst: "4" }
        - { epochs: "002", epoch: "008", inst: "5" }
        - { epochs: "002", epoch: "008", inst: "6" }
    - - name: run-aggregation-jobs-04
        template: aggregation-job
        arguments:
          parameters:
          - name: round
            value: "{{steps.run-aggregation-jobs-03.outputs.parameters.next-round}}"
          - name: algorithm
            value: "fedavg"
          - name: inst-id
            value: "0"
          - name: model_pth
            value: "None"
  # round 5
    - - name: run-train-jobs-05
        template: train-job
        arguments:
          parameters:
          - name: round
            value: "{{steps.run-aggregation-jobs-04.outputs.parameters.next-round}}"
          - name: epochs
            value: "{{item.epochs}}"
          - name: epoch
            value: "{{item.epoch}}"
          - name: inst-id
            value: "{{item.inst}}"
          - name: model_pth
            value: "/fedpod/states/{{workflow.parameters.job-prefix}}_0/R{{workflow.parameters.rounds}}r{{steps.run-aggregation-jobs-04.outputs.parameters.next-round}}/models/R{{workflow.parameters.rounds}}r{{steps.run-aggregation-jobs-04.outputs.parameters.next-round}}.pth"
        withItems:
        - { epochs: "002", epoch: "010", inst: "1" }
        - { epochs: "002", epoch: "010", inst: "2" }
        - { epochs: "002", epoch: "010", inst: "3" }
        - { epochs: "002", epoch: "010", inst: "4" }
        - { epochs: "002", epoch: "010", inst: "5" }
        - { epochs: "002", epoch: "010", inst: "6" }
    - - name: run-aggregation-jobs-05
        template: aggregation-job
        arguments:
          parameters:
          - name: round
            value: "{{steps.run-aggregation-jobs-04.outputs.parameters.next-round}}"
          - name: algorithm
            value: "fedavg"
          - name: inst-id
            value: "0"
          - name: model_pth
            value: "None"
  # round 6
    - - name: run-train-jobs-06
        template: train-job
        arguments:
          parameters:
          - name: round
            value: "{{steps.run-aggregation-jobs-05.outputs.parameters.next-round}}"
          - name: epochs
            value: "{{item.epochs}}"
          - name: epoch
            value: "{{item.epoch}}"
          - name: inst-id
            value: "{{item.inst}}"
          - name: model_pth
            value: "/fedpod/states/{{workflow.parameters.job-prefix}}_0/R{{workflow.parameters.rounds}}r{{steps.run-aggregation-jobs-05.outputs.parameters.next-round}}/models/R{{workflow.parameters.rounds}}r{{steps.run-aggregation-jobs-05.outputs.parameters.next-round}}.pth"
        withItems:
        - { epochs: "002", epoch: "012", inst: "1" }
        - { epochs: "002", epoch: "012", inst: "2" }
        - { epochs: "002", epoch: "012", inst: "3" }
        - { epochs: "002", epoch: "012", inst: "4" }
        - { epochs: "002", epoch: "012", inst: "5" }
        - { epochs: "002", epoch: "012", inst: "6" }
    - - name: run-aggregation-jobs-06
        template: aggregation-job
        arguments:
          parameters:
          - name: round
            value: "{{steps.run-aggregation-jobs-05.outputs.parameters.next-round}}"
          - name: algorithm
            value: "fedavg"
          - name: inst-id
            value: "0"
          - name: model_pth
            value: "None"
  # round 7
    - - name: run-train-jobs-07
        template: train-job
        arguments:
          parameters:
          - name: round
            value: "{{steps.run-aggregation-jobs-06.outputs.parameters.next-round}}"
          - name: epochs
            value: "{{item.epochs}}"
          - name: epoch
            value: "{{item.epoch}}"
          - name: inst-id
            value: "{{item.inst}}"
          - name: model_pth
            value: "/fedpod/states/{{workflow.parameters.job-prefix}}_0/R{{workflow.parameters.rounds}}r{{steps.run-aggregation-jobs-06.outputs.parameters.next-round}}/models/R{{workflow.parameters.rounds}}r{{steps.run-aggregation-jobs-06.outputs.parameters.next-round}}.pth"
        withItems:
        - { epochs: "002", epoch: "014", inst: "1" }
        - { epochs: "002", epoch: "014", inst: "2" }
        - { epochs: "002", epoch: "014", inst: "3" }
        - { epochs: "002", epoch: "014", inst: "4" }
        - { epochs: "002", epoch: "014", inst: "5" }
        - { epochs: "002", epoch: "014", inst: "6" }
    - - name: run-aggregation-jobs-07
        template: aggregation-job
        arguments:
          parameters:
          - name: round
            value: "{{steps.run-aggregation-jobs-06.outputs.parameters.next-round}}"
          - name: algorithm
            value: "fedavg"
          - name: inst-id
            value: "0"
          - name: model_pth
            value: "None"
  # round 8
    - - name: run-train-jobs-08
        template: train-job
        arguments:
          parameters:
          - name: round
            value: "{{steps.run-aggregation-jobs-07.outputs.parameters.next-round}}"
          - name: epochs
            value: "{{item.epochs}}"
          - name: epoch
            value: "{{item.epoch}}"
          - name: inst-id
            value: "{{item.inst}}"
          - name: model_pth
            value: "/fedpod/states/{{workflow.parameters.job-prefix}}_0/R{{workflow.parameters.rounds}}r{{steps.run-aggregation-jobs-07.outputs.parameters.next-round}}/models/R{{workflow.parameters.rounds}}r{{steps.run-aggregation-jobs-07.outputs.parameters.next-round}}.pth"
        withItems:
        - { epochs: "002", epoch: "016", inst: "1" }
        - { epochs: "002", epoch: "016", inst: "2" }
        - { epochs: "002", epoch: "016", inst: "3" }
        - { epochs: "002", epoch: "016", inst: "4" }
        - { epochs: "002", epoch: "016", inst: "5" }
        - { epochs: "002", epoch: "016", inst: "6" }
    - - name: run-aggregation-jobs-08
        template: aggregation-job
        arguments:
          parameters:
          - name: round
            value: "{{steps.run-aggregation-jobs-07.outputs.parameters.next-round}}"
          - name: algorithm
            value: "fedavg"
          - name: inst-id
            value: "0"
          - name: model_pth
            value: "None"
  # round 9
    - - name: run-train-jobs-09
        template: train-job
        arguments:
          parameters:
          - name: round
            value: "{{steps.run-aggregation-jobs-08.outputs.parameters.next-round}}"
          - name: epochs
            value: "{{item.epochs}}"
          - name: epoch
            value: "{{item.epoch}}"
          - name: inst-id
            value: "{{item.inst}}"
          - name: model_pth
            value: "/fedpod/states/{{workflow.parameters.job-prefix}}_0/R{{workflow.parameters.rounds}}r{{steps.run-aggregation-jobs-08.outputs.parameters.next-round}}/models/R{{workflow.parameters.rounds}}r{{steps.run-aggregation-jobs-08.outputs.parameters.next-round}}.pth"
        withItems:
        - { epochs: "002", epoch: "018", inst: "1" }
        - { epochs: "002", epoch: "018", inst: "2" }
        - { epochs: "002", epoch: "018", inst: "3" }
        - { epochs: "002", epoch: "018", inst: "4" }
        - { epochs: "002", epoch: "018", inst: "5" }
        - { epochs: "002", epoch: "018", inst: "6" }
    - - name: run-aggregation-jobs-09
        template: aggregation-job
        arguments:
          parameters:
          - name: round
            value: "{{steps.run-aggregation-jobs-08.outputs.parameters.next-round}}"
          - name: algorithm
            value: "fedavg"
          - name: inst-id
            value: "0"
          - name: model_pth
            value: "None"
  # round 10
    - - name: run-train-jobs-10
        template: train-job
        arguments:
          parameters:
          - name: round
            value: "{{steps.run-aggregation-jobs-09.outputs.parameters.next-round}}"
          - name: epochs
            value: "{{item.epochs}}"
          - name: epoch
            value: "{{item.epoch}}"
          - name: inst-id
            value: "{{item.inst}}"
          - name: model_pth
            value: "/fedpod/states/{{workflow.parameters.job-prefix}}_0/R{{workflow.parameters.rounds}}r{{steps.run-aggregation-jobs-09.outputs.parameters.next-round}}/models/R{{workflow.parameters.rounds}}r{{steps.run-aggregation-jobs-09.outputs.parameters.next-round}}.pth"
        withItems:
        - { epochs: "001", epoch: "020", inst: "1" }
        - { epochs: "001", epoch: "020", inst: "2" }
        - { epochs: "001", epoch: "020", inst: "3" }
        - { epochs: "001", epoch: "020", inst: "4" }
        - { epochs: "001", epoch: "020", inst: "5" }
        - { epochs: "001", epoch: "020", inst: "6" }
    - - name: run-aggregation-jobs-10
        template: aggregation-job
        arguments:
          parameters:
          - name: round
            value: "{{steps.run-aggregation-jobs-09.outputs.parameters.next-round}}"
          - name: algorithm
            value: "fedavg"
          - name: inst-id
            value: "0"
          - name: model_pth
            value: "None"
  # round 11
    - - name: run-train-jobs-11
        template: train-job
        arguments:
          parameters:
          - name: round
            value: "{{steps.run-aggregation-jobs-10.outputs.parameters.next-round}}"
          - name: epochs
            value: "{{item.epochs}}"
          - name: epoch
            value: "{{item.epoch}}"
          - name: inst-id
            value: "{{item.inst}}"
          - name: model_pth
            value: "/fedpod/states/{{workflow.parameters.job-prefix}}_0/R{{workflow.parameters.rounds}}r{{steps.run-aggregation-jobs-10.outputs.parameters.next-round}}/models/R{{workflow.parameters.rounds}}r{{steps.run-aggregation-jobs-10.outputs.parameters.next-round}}.pth"
        withItems:
        - { epochs: "001", epoch: "021", inst: "1" }
        - { epochs: "001", epoch: "021", inst: "2" }
        - { epochs: "001", epoch: "021", inst: "3" }
        - { epochs: "001", epoch: "021", inst: "4" }
        - { epochs: "001", epoch: "021", inst: "5" }
        - { epochs: "001", epoch: "021", inst: "6" }
    - - name: run-aggregation-jobs-11
        template: aggregation-job
        arguments:
          parameters:
          - name: round
            value: "{{steps.run-aggregation-jobs-10.outputs.parameters.next-round}}"
          - name: algorithm
            value: "fedavg"
          - name: inst-id
            value: "0"
          - name: model_pth
            value: "None"
  # round 12
    - - name: run-train-jobs-12
        template: train-job
        arguments:
          parameters:
          - name: round
            value: "{{steps.run-aggregation-jobs-11.outputs.parameters.next-round}}"
          - name: epochs
            value: "{{item.epochs}}"
          - name: epoch
            value: "{{item.epoch}}"
          - name: inst-id
            value: "{{item.inst}}"
          - name: model_pth
            value: "/fedpod/states/{{workflow.parameters.job-prefix}}_0/R{{workflow.parameters.rounds}}r{{steps.run-aggregation-jobs-11.outputs.parameters.next-round}}/models/R{{workflow.parameters.rounds}}r{{steps.run-aggregation-jobs-11.outputs.parameters.next-round}}.pth"
        withItems:
        - { epochs: "001", epoch: "022", inst: "1" }
        - { epochs: "001", epoch: "022", inst: "2" }
        - { epochs: "001", epoch: "022", inst: "3" }
        - { epochs: "001", epoch: "022", inst: "4" }
        - { epochs: "001", epoch: "022", inst: "5" }
        - { epochs: "001", epoch: "022", inst: "6" }
    - - name: run-aggregation-jobs-12
        template: aggregation-job
        arguments:
          parameters:
          - name: round
            value: "{{steps.run-aggregation-jobs-11.outputs.parameters.next-round}}"
          - name: algorithm
            value: "fedavg"
          - name: inst-id
            value: "0"
          - name: model_pth
            value: "None"
  # round 13
    - - name: run-train-jobs-13
        template: train-job
        arguments:
          parameters:
          - name: round
            value: "{{steps.run-aggregation-jobs-12.outputs.parameters.next-round}}"
          - name: epochs
            value: "{{item.epochs}}"
          - name: epoch
            value: "{{item.epoch}}"
          - name: inst-id
            value: "{{item.inst}}"
          - name: model_pth
            value: "/fedpod/states/{{workflow.parameters.job-prefix}}_0/R{{workflow.parameters.rounds}}r{{steps.run-aggregation-jobs-12.outputs.parameters.next-round}}/models/R{{workflow.parameters.rounds}}r{{steps.run-aggregation-jobs-12.outputs.parameters.next-round}}.pth"
        withItems:
        - { epochs: "001", epoch: "023", inst: "1" }
        - { epochs: "001", epoch: "023", inst: "2" }
        - { epochs: "001", epoch: "023", inst: "3" }
        - { epochs: "001", epoch: "023", inst: "4" }
        - { epochs: "001", epoch: "023", inst: "5" }
        - { epochs: "001", epoch: "023", inst: "6" }
    - - name: run-aggregation-jobs-13
        template: aggregation-job
        arguments:
          parameters:
          - name: round
            value: "{{steps.run-aggregation-jobs-12.outputs.parameters.next-round}}"
          - name: algorithm
            value: "fedavg"
          - name: inst-id
            value: "0"
          - name: model_pth
            value: "None"
  # round 14
    - - name: run-train-jobs-14
        template: train-job
        arguments:
          parameters:
          - name: round
            value: "{{steps.run-aggregation-jobs-13.outputs.parameters.next-round}}"
          - name: epochs
            value: "{{item.epochs}}"
          - name: epoch
            value: "{{item.epoch}}"
          - name: inst-id
            value: "{{item.inst}}"
          - name: model_pth
            value: "/fedpod/states/{{workflow.parameters.job-prefix}}_0/R{{workflow.parameters.rounds}}r{{steps.run-aggregation-jobs-13.outputs.parameters.next-round}}/models/R{{workflow.parameters.rounds}}r{{steps.run-aggregation-jobs-13.outputs.parameters.next-round}}.pth"
        withItems:
        - { epochs: "001", epoch: "024", inst: "1" }
        - { epochs: "001", epoch: "024", inst: "2" }
        - { epochs: "001", epoch: "024", inst: "3" }
        - { epochs: "001", epoch: "024", inst: "4" }
        - { epochs: "001", epoch: "024", inst: "5" }
        - { epochs: "001", epoch: "024", inst: "6" }
    - - name: run-aggregation-jobs-14
        template: aggregation-job
        arguments:
          parameters:
          - name: round
            value: "{{steps.run-aggregation-jobs-13.outputs.parameters.next-round}}"
          - name: algorithm
            value: "fedavg"
          - name: inst-id
            value: "0"
          - name: model_pth
            value: "None"





  - name: train-job
    inputs:
      parameters:
      - name: round
      - name: epochs
      - name: epoch
      - name: inst-id
      - name: model_pth
    container:
      image: dwnusa/fedpod:v0.4.14.18
      command: ["/bin/sh", "-c"]
      args: ["./run_train1.sh -s 1 -g 0 -J {{workflow.parameters.job-prefix}}_{{inputs.parameters.inst-id}} -R {{workflow.parameters.rounds}} -r {{inputs.parameters.round}} -E {{inputs.parameters.epochs}} -e {{inputs.parameters.epoch}} -i {{inputs.parameters.inst-id}} -c {{workflow.parameters.split_csv}} -m {{inputs.parameters.model_pth}}"]
      resources:
        requests:
          cpu: "2"
          memory: "16G"
      volumeMounts:
      - name: cc359ppmi128-volume
        mountPath: /fedpod/cc359ppmi128
      - name: states-volume
        mountPath: /fedpod/states
      - name: logs-volume
        mountPath: /fedpod/logs
      stdin: true
      tty: true
    volumes:
    - name: cc359ppmi128-volume
      hostPath:
        path: /fedpod/cc359ppmi128  # 로컬 경로
        type: Directory
    - name: states-volume
      hostPath:
        path: /fedpod/states  # 로컬 경로
        type: Directory
    - name: logs-volume
      hostPath:
        path: /fedpod/logs  # 로컬 경로
        type: Directory


  - name: aggregation-job
    inputs:
      parameters:
      - name: round
      - name: algorithm
      - name: inst-id
      - name: model_pth
    container:
      image: dwnusa/fedpod:v0.4.14.18
      command: ["/bin/sh", "-c"]
      args: ["./run_aggregation.sh -R {{workflow.parameters.rounds}} -r {{inputs.parameters.round}} -a {{inputs.parameters.algorithm}} -j {{workflow.parameters.job-prefix}} -i {{inputs.parameters.inst-id}} -m {{inputs.parameters.model_pth}}"]
      # args: ["./run_aggregation.sh -r 0 -R 5 -a fedavg -j inst -i 0 -m /fedpod/cc359ppmi128/R00E000.pth"]
      resources:
        requests:
          cpu: "2"
          memory: "4G"
      volumeMounts:
      - name: cc359ppmi128-volume
        mountPath: /fedpod/cc359ppmi128
      - name: states-volume
        mountPath: /fedpod/states
      - name: logs-volume
        mountPath: /fedpod/logs
      stdin: true
      tty: true
    outputs:
      parameters:
      - name: next-round
        valueFrom:
          path: /tmp/next_round.txt
      - name: next-epoch
        valueFrom:
          path: /tmp/next_epoch.txt
    volumes:
    - name: cc359ppmi128-volume
      hostPath:
        path: /fedpod/cc359ppmi128  # 로컬 경로
        type: Directory
    - name: states-volume
      hostPath:
        path: /fedpod/states  # 로컬 경로
        type: Directory
    - name: logs-volume
      hostPath:
        path: /fedpod/logs  # 로컬 경로
        type: Directory